{
  "engines": {
    "node": "20.2.0"
  },
  "cacheDirectories": [
    "ui/node_modules"
  ],
  "scripts": {
    "heroku-postbuild": "cd ui && npm install && npm install --only=dev && npm run build"
  }
}